{
  "name": "run-lhci-server",
  "version": "0.0.1",
  "description": "Lighthouse CI Server Docker image for Cloud Run (+ Cloud SQL)",
  "main": "index.js",
  "scripts": {
    "start": "lhci server --config=./lighthouserc.js"
  },
  "license": "MIT",
  "dependencies": {
    "@lhci/cli": "^0.8.0",
    "@lhci/server": "^0.8.0",
    "mysql2": "^2.2.5"
  }
}
